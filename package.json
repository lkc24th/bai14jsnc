{
  "name": "bai14-reading-list-crud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "json-server --watch db.json --port 3000",
    "start": "npm run server"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
